name: push-to-blob-on-push
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  run-shell-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x ./upload-blob.sh
      - run: ./upload-blob.sh
